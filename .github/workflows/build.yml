name: build

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'cmd/**'
      - 'Makefile'
      - '.golangci.yml'
      - '.github/workflows/build.yml'
  pull_request:
    branches: [ main ]
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'cmd/**'
      - 'Makefile'
      - '.golangci.yml'
      - '.github/workflows/build.yml'
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Only run on non-draft PRs
    if: github.event.pull_request.draft == false || github.event_name == 'push'
    strategy:
      # Test only latest Go version on PRs, all versions on main
      matrix:
        go-version: ${{ github.event_name == 'pull_request' && fromJSON('["1.23"]') || fromJSON('["1.21", "1.22", "1.23"]') }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ matrix.go-version }}

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download

    - name: Run go vet
      run: go vet ./...

    - name: Run golangci-lint
      uses: golangci/golangci-lint-action@v7
      with:
        version: v2.7.2
        args: --timeout=5m

    - name: Run tests
      run: make test

    - name: Get version
      id: version
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
      run: |
        VERSION=$(git describe --tags --always --dirty 2>/dev/null | sed 's/^v//' || echo "dev")
        echo "version=$VERSION" >> $GITHUB_OUTPUT

    - name: Build
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
      run: |
        mkdir -p bin
        go build -o bin/fs.tool-linux-amd64 ./cmd/fs

    - name: Upload artifacts
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
      uses: actions/upload-artifact@v4
      with:
        name: fs.tool-linux-amd64
        path: bin/fs.tool-linux-amd64
        retention-days: 7
