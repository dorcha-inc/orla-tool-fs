name: readfile
version: 0.1.3
description: Read and return the contents of a file
entrypoint: bin/readfile.py
author: dorcha-inc
license: MIT
repository: https://github.com/dorcha-inc/orla-tool-file-read
keywords:
  - file
  - read
  - filesystem
  - io
mcp:
  input_schema:
    type: object
    properties:
      path:
        type: string
        description: Path to the file to read
    required:
      - path

